export const metadata = () => {
  return {
    title: "Sign Up",
    description: "This is the Sign Up Page",
  };
};

const layout = ({ children }) => {
  return <>{children}</>;
};

export default layout;
