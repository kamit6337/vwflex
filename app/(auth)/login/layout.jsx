export const metadata = () => {
  return {
    title: "Login",
    description: "This is the Login Page",
  };
};

const layout = ({ children }) => {
  return <>{children}</>;
};

export default layout;
